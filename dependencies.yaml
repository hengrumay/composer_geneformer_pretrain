version: "4"
dependencies:
  # Testing with PyTorch 2.8 - using Composer from git main for torch 2.8 support
  - torch==2.8.0
  - torchvision>=0.20.0
  # Install Composer from git main (0.32.1 requires torch<2.7.1)
  - git+https://github.com/mosaicml/composer.git@main
  - mlflow>=2.14.1
  - mosaicml-streaming==0.9.0
  - transformers==4.44.0
  - datasets==2.21.0
  - boto3
  - omegaconf
  - tqdm

# Note: mosaicml 0.32.1 requires torch<2.7.1,>=2.6.0, so using git main for torch 2.8 support
# This is experimental - may have stability issues