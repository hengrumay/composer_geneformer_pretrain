version: "4"
dependencies:
  - mosaicml[mlflow]==0.23.5
  - mosaicml-streaming==0.8.0
  - transformers==4.44.0
  - datasets==2.21.0
  - boto3
  - omegaconf
  - tqdm
#- transformers
#- accelerate


# Based on the previous run logs, mosaicml==0.23.5 installed torch 2.3.1:
# torch-2.3.1 torchvision-0.18.1

# This makes sense because:
# mosaicml 0.23.5 was released around mid-2024
# PyTorch 2.3.1 was the stable version at that time
# The CUDA 12.1 libraries were also installed (nvidia-cuda-*-cu12-12.1.*)