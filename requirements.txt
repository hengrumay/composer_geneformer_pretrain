# NOTE: On Databricks Serverless GPU, we install with `--no-deps` in commands.sh.
# This avoids pip resolving/downgrading Torch and core Databricks runtime packages.
# Because of that, we must list key runtime deps explicitly (HF deps, Composer deps).

# Composer (training loop). On Databricks Serverless GPU we run Python 3.12, and older MosaicML
# releases may not ship py312 wheels. We therefore do NOT hard-pin mosaicml here.
# Instead, commands.sh will ensure `composer` is importable by attempting:
#   pip install --no-deps composer
#   (fallback) pip install --no-deps mosaicml
# The core python deps are listed below because we install with --no-deps.
coolname<3,>=1.1.0
tabulate==0.9.0
py-cpuinfo<10,>=8.0.0
torchmetrics>=0.10.0,<1.3.3
torch-optimizer<0.4,>=0.3.0
pytorch-ranger>=0.1.1
lightning-utilities>=0.7.0

mosaicml-streaming==0.8.0
transformers==4.44.0
datasets==2.21.0
# Explicit HF deps (we install with --no-deps in commands.sh)
huggingface-hub>=0.23.2,<1.0
tokenizers>=0.19,<0.20
safetensors>=0.4.1
regex!=2019.12.17
filelock
triton
boto3
omegaconf
tqdm



