# NOTE: On Databricks Serverless GPU, avoid installing `mosaicml` / `mosaicml[mlflow]` at runtime.
# They can downgrade torch and/or override Databricks runtime packages, which breaks multi-node.
mosaicml-streaming==0.8.0
transformers==4.44.0
datasets==2.21.0
triton
boto3
omegaconf
tqdm



