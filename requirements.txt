# Training stack
composer==0.32.1
mosaicml-streaming==0.8.0

# HF + data
transformers==4.44.0
datasets==2.21.0

# AWS + config
boto3
omegaconf
tqdm
pyyaml

# Optional pins (fine to keep for determinism; mosaicml will pull what it needs anyway)
torchmetrics>=0.10.0,<1.3.3
torch-optimizer<0.4,>=0.3.0
pytorch-ranger>=0.1.1
lightning-utilities>=0.7.0
tabulate==0.9.0
coolname<3,>=1.1.0
py-cpuinfo<10,>=8.0.0



