mosaicml==0.23.5
# NOTE: Do NOT install mosaicml[mlflow] on Databricks Serverless GPU.
# It pulls/pins mlflow + databricks-sdk versions that can override the runtime and
# cause MLflow "System metrics" to not populate reliably.
mosaicml-streaming==0.8.0
transformers==4.44.0
datasets==2.21.0
triton
boto3
omegaconf
tqdm



