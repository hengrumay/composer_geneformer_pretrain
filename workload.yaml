experiment_name: geneformer-sgcli-mmt-test

workspace:
  host: https://e2-dogfood.staging.cloud.databricks.com/

repo_snapshot:
  local_repo_path: /Users/may.merkletan/Documents/Projects/Merck/composer_geneformer_pretrain

environment:
  dependencies: requirements.yaml
  env_variables:
    NCCL_DEBUG: "WARN"

    # Enable MLflow system metrics as early as possible.
    MLFLOW_ENABLE_SYSTEM_METRICS_LOGGING: "true"
    # Torchrun multi-node (classic cluster, 1 GPU per A10 node)
    NNODES: "8"
    MASTER_PORT: "29400"

compute:
  gpus: 8
  gpu_type: a10

bash_script: sgcli_entrypoint.sh
max_retries: 0

